/-
Ce fichier réunit tous les mots clef introduits par les tactiques bavardes. 
Ces mots ne peuvent plus servir à désigner des lemmes, objets ou hypothèses.
-/

precedence `applique`:0
precedence `appliqué`:0
precedence `à`:0
precedence `on`:0
precedence `obtient`:0
precedence `choisit`:0
precedence `que`:0
precedence `tel`:0
precedence `convient`:0
precedence `une`:0
precedence `contradiction`:0
precedence `conclut`:0
precedence `par`:0
precedence `calcule`:0
precedence `dans`:0
precedence `combine`:0
precedence `remplace`:0
precedence `partout`:0
precedence `contrapose`:0
precedence `simplement`:0
precedence `discute`:0
precedence `selon`:0
precedence `il`:0
precedence `suffit`:0
precedence `de`:0
precedence `montrer`:0
precedence `déplie`:0
precedence `l'absurde`:0
precedence `renomme`:0
precedence `en`:0
precedence `oublie`:0
precedence `reformule`:0
precedence `récurrence`:0
precedence `pousse`:0
precedence `la`:0
precedence `négation`:0
precedence `utilisant`:0
precedence `qui`:0
precedence `devient`:0